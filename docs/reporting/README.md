# Reporting presentation design package

**Status: draft, awaiting owner approval of the golden sample. Docs only — no product
code changed. Implementation must not begin until the golden sample is approved.**

## Why this package exists

Owner review of the generated HTML, PDF, and Excel outputs on 2026-08-04 found that
they present technical data to a customer: raw metric identifiers, figure and citation
identifiers, unit-kind codes, section states, refusal codes, caveat codes, and full
bundle provenance — all inside the primary report, styled as machine identifiers.

The initial diagnosis was an incomplete wording table (`rendering/wording.py` words 7
metrics and 0 refusal reasons). That diagnosis was too narrow. The outputs are
**structurally** audit ledgers — ordered by how Khepri computed things, not by what a
reader needs to decide. Translating the identifiers in place produces a translated
technical report.

The fix is two explicitly separated presentation layers.

## Read in this order

| # | Document | Answers |
|---|---|---|
| 1 | [`presentation-visibility-matrix.md`](presentation-visibility-matrix.md) | Which fields may appear where — Business / Audit / Internal |
| 2 | [`business-report-information-architecture.md`](business-report-information-architecture.md) | What the customer report *is*, per surface |
| 3 | [`refusal-presentation.md`](refusal-presentation.md) | How a refusal reaches a customer; the 13-code catalog |
| 4 | [`golden-sample/`](golden-sample/) | All three surfaces, one fictional dataset |

## The golden sample

Fictional client "Al Rahma Trading Co.", January–July 2026. Every number is invented.

| File | What to look for |
|---|---|
| `khepri-sales-review-sample.html` | Nine business sections, then a Technical Evidence section below a divider |
| `khepri-sales-review-sample.pdf` | 8 pages; business body, then the appendix on a fresh page |
| `khepri-sales-review-sample.xlsx` | 12 sheets; nine business, one hidden chartdata, two audit |

The workbook was generated by a throwaway script in the session scratchpad, not by
product code. The PDF was rendered by Chromium and passes all four of `pdf.py`'s
publication guards (`%PDF-` header, `%%EOF` trailer, three tagged markers, embedded
font program).

### What makes it "rich" — and the constraint on that

The sample uses **all twelve governed metrics**: the ten in `facts.py:69-78` plus the
two growth effects in `analysis/growth.py:72-73`. The first draft used four, which is
why it read thin — `cost`, `gross_profit`, `gross_margin`, `discount`, `returns` and
`average_selling_price` are all computable today and were reaching the page as raw
identifiers or not at all.

Richness comes from metrics Khepri **already computes**, never from invented analysis.
A golden sample is a promise about the product; inventing a forecast, a customer
segmentation, or a basket-affinity matrix would make the sample a specification for
work nobody approved — the same over-promising, in the opposite direction. The one
metric the sample deliberately does *not* show is comparable-store sales, which is
recorded as a roadmap item instead (see below).

Depth added: per-month margin trend, per-branch revenue/units/margin/change,
discount-and-return leakage as a share of revenue, and seven "what this means"
insight callouts that read the figures against each other rather than restating them.

### Mechanically verified

The separation rule is decidable, so it was checked rather than eyeballed. No cell or
visible text in a business surface may contain a snake_case token, a `sha256:` digest,
an id-prefixed code, or a module path:

| Surface | Business region | Audit region |
|---|---|---|
| HTML | **0** identifiers | 46 identifiers |
| Excel | **0** identifiers (9 business sheets) | 47 identifiers (Audit Trail + Provenance) |

Also verified: **zero** Eastern-Arabic numerals (§B.4a), and the Chart Data sheet
carries `state="hidden"` — present so the native charts resolve, absent from the tabs.

This check should become a test when the implementation slice lands. Match identifier
*tokens*, not words — an early version of the check flagged "Total revenue change"
because it contains the word `revenue`, which is also a metric id. The business name
and the identifier usually share a root; that is expected, not a leak.

## Three findings that change scope

1. **The customer-facing refusal catalog is 13, not 32.** 8 section reasons
   (`GOVERNED_SECTION_REASONS`) + 5 result reasons (`facts.py:80-84`). The ~21
   `GOVERNED_REASONS` are bundle-integrity codes: when one fires **no report is
   published**, so a customer cannot encounter one. Earlier sessions counted all three
   vocabularies together and used "32" as a product differentiator. The differentiator
   survives; the count does not.

2. **The separation is a pure presentation change.** `reconcile`
   (`bundle.py:1271-1314`) validates the `SurfaceContent` claim and never parses the
   document. No change to the reconcile contract is proposed or required.

3. **Caveat wording is mandatory and complete, not incremental.** `bundle.py:1324`
   requires claimed and bundle caveat sets to be *equal*. An unworded caveat is a
   reconcile failure.

## Delivery: business report always, audit evidence on request

**Owner decision, 2026-08-04.** The business report is the product. The audit
evidence is optional *to the customer*.

"Optional" means **delivery only**. Tier A content is generated for every report,
unconditionally, for two reasons:

- `REQUIRED_SURFACES` is compared for exact equality in six places, so a bundle
  producing fewer than three surfaces returns `REASON_MISSING_SURFACE` and is an
  *incomplete bundle* — no report at all (`bundle.py:1222`).
- `surface_digest` content-addresses what each surface presented. Conditional
  generation would make the digest vary with a customer preference rather than
  with the data, and "the same input reproduces the same bundle identity" is the
  claim the product is sold on.

`delivery_persistence.py:239` already reads surfaces back per-surface with an
`if surface in found` guard, so withholding one from a download needs no contract
change. **Do not implement optionality by skipping generation.**

The business report names its reference and points to the evidence once, in the
colophon, and carries no identifier anywhere else.

## Decisions taken, 2026-08-04

| Question | Adopted |
|---|---|
| Excel "Product & Category" sheet | **(a)** re-present the concentration family's ranked buckets — no new governed analysis |
| Bilingual workbook naming | **(a)** keep the `(English)` / `(العربية)` suffix, one workbook |
| Audit layer optionality | Generated always, **delivered on request** |
| Business report reference | Short reference **plus** a pointer to the evidence |
| Comparable-store sales | **Roadmap Phase 3**, not this package — new governed analysis, needs two comparable periods |

## Scope boundary

Docs only. No `src/` edits, no registry edits, no new decision record. If the
implementation slice turns out to need a governance change, that is a flagged
dependency in the IA (§B.6) — currently none is known.
