# FAMILY-ID: Name

## Owns

List the responsibilities this family uniquely owns.

## Excludes

List product, data, infrastructure, and other-family boundaries explicitly.

Add the document to `governance/registry.yaml` as an `active` or `retired` family.
